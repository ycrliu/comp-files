#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int x[1005];

int main()
{
	if(fopen("race.in", "r"))
	{
		freopen("race.in", "r", stdin);
		freopen("race.out", "w", stdout);
	}
	int k, n;
	cin >> k >> n;
	for(int i = 0; i < n; i++)
		cin >> x[i];
	
	return 0;
}

